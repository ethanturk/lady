{
  "identifier": "desktop",
  "description": "Desktop-only capabilities (auto-updater). Scoped to macOS/Windows/Linux so mobile builds never reference the updater permission.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": [
    "updater:default"
  ]
}
