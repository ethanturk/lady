# Phase 6 — Polish & ship (the GA ship: v1.3.0)

Branch: phase-6-polish-ship (from main; Phase 5 merged at 1.2.0).

Loop verification per story: cargo fmt --all --check, cargo clippy --all-targets
-- -D warnings, cargo test, cargo deny check, npm run build (UI stories). No new
product features — this is packaging + quality + the ship. Secrets (signing
certs, notarization creds, updater private key) live in CI/keychain, NEVER in the
repo; only the updater PUBLIC key is committed.

## Status
- [x] PH6-001 Performance benchmarks + budgets
- [x] PH6-002 Large-repo performance passes
- [x] PH6-003 Accessibility pass
- [x] PH6-004 Theming polish
- [x] PH6-005 Packaging — macOS (.dmg/.app, notarized)
- [x] PH6-006 Packaging — Windows (.msi/NSIS, Authenticode)
- [x] PH6-007 Packaging — Linux (AppImage + Flatpak)
- [x] PH6-008 Auto-update (Tauri updater + signed manifests)
- [x] PH6-009 Documentation + release notes
- [x] PH6-010 Release CI pipeline
- [x] PH6-011 Phase 6 GA exit gate (v1.3.0 ship)

## Notes (as stories land)
- PH6-001/002: new dev-only `lady-fixtures` crate — seeded, offline synthetic
  generators (xorshift64, no rand): synthetic_commits (graph), synthetic_text
  (diff), build_synthetic_repo (real repo via system git for log-walk + status).
  criterion benches (default-features off, cargo_bench_support only → deny stays
  clean): lady-graph/benches/layout.rs, lady-diff/benches/diff.rs,
  lady-git/benches/repo_ops.rs. `[lib] bench = false` on the three so `cargo
  bench` runs only criterion targets (libtest rejects --quick). CI `bench` job
  compiles + runs --quick (non-gating). docs/PERF.md records budgets + measured
  numbers (dev machine, Apple Silicon): graph_layout 100→14µs / 10k→1.37ms;
  text_diff 500→44µs / 20k→2.0ms; walk_log/512→22ms; status→11ms — all inside
  the §8.9 budgets (60fps frame, <100ms warm refresh, <1s cold open). PH6-002:
  verified incremental layout (layout_continuation + ActiveLanes), virtualized
  rows (GraphView/CommitList visibleSlice window) and paged walk_log
  (WalkLogQuery limit/start, walk_log_graph carries lane state) — no budget
  misses, no optimization required. Residual: walk_log is linear in limit; UI
  keeps paging. Whole gate green (fmt/clippy/test/deny).
- PH6-004 (theming): added semantic tokens (success/warning/danger/info +
  bg/border variants, accent-2, focus-ring) to both themes in styles.css, all
  AA-contrast. Audited Phases 1-5 views — replaced ~93 hard-coded hex with
  var(--token); only GraphView's intentional categorical lane palette remains
  (commit-dot ring now reads resolved --bg). Added an optional teal accent
  (data-accent) + accent toggle in ThemeToggle to prove the token system.
  docs/THEMING.md documents the token set + audit + extra theme.
- PH6-003 (a11y): global :focus-visible ring + .sr-only helper +
  prefers-reduced-motion block in styles.css. App.tsx: role=tablist/tab +
  aria-selected (tabProps helper), role=main, live regions (role=alert/status)
  for errors/notices/conflicts. AiView: sr-only "AI generating" status +
  aria-live on output. CommandPalette: role=dialog/combobox/listbox/option +
  aria-activedescendant (already keyboard-driven). ThemeToggle: aria-labels.
  docs/ACCESSIBILITY.md documents the AA contrast table + manual VoiceOver/NVDA
  pass. npm build green; Rust untouched (UI-only). KEYBOARD.md ref lands in
  PH6-009.
- PH6-005/006/007 (packaging): tauri.conf.json bundle.active=true, targets=all,
  full icon set generated (icns/ico/png) via `cargo tauri icon`, category +
  updater artifacts. macOS .app/.dmg (Developer ID sign+notarize via CI env),
  Windows .msi/NSIS (Authenticode via CI cert/Azure Trusted Signing), Linux
  AppImage + a Flatpak manifest (flatpak/dev.lady.client.yml + .desktop,
  org.gnome.Platform→WebKitGTK). Signing all CI-secret-gated, skipped without
  secrets. docs/PACKAGING.md.
- PH6-008 (auto-update): tauri-plugin-updater wired (.plugin + updater:default
  capability + plugins.updater config: committed PUBLIC key + GitHub-Releases
  endpoint). src-tauri/src/updater.rs: check_for_updates/install_update commands
  (explicit user action, Settings→Updates), no silent update. minisign keypair
  generated; PRIVATE key kept in /tmp (gitignored *.key), PUBLIC key committed.
  3 verify tests vs committed fixtures (latest.json + .sig): valid sig verifies,
  tampered manifest rejected, config pubkey matches const. deny clean
  (minisign-verify/tauri-plugin-updater).
- PH6-010 (release CI): .github/workflows/release.yml — v* tag or dispatch
  dry-run; gate job (fmt/clippy/test/deny+npm) → tauri-action matrix bundle/
  sign/notarize/publish + signed latest.json → best-effort flatpak leg. Fork-
  safe (no secrets→unsigned, no publish). PR ci.yml unchanged (+ added the
  non-gating bench job in PH6-001).
- PH6-009 (docs): docs/USER-GUIDE.md, KEYBOARD.md, AI-PRIVACY.md (BYOK/consent/
  best-effort redaction/Ollama — ADR-0008/0009), MCP.md (lady-mcp 6 read-only
  tools + config), and CHANGELOG.md (v1.0→1.3.0). All intra-doc links resolve;
  no secrets in docs.
- PH6-011 (GA exit gate): bumped version 1.2.0→1.3.0 across all 11 crate
  Cargo.tomls + src-tauri/tauri.conf.json + ui/package.json (Cargo.lock
  refreshed). docs/RELEASE-CHECKLIST.md gains the Phase 6 GA table (each item →
  its story + doc), the GA ship line, the green-gate (incl. benches + release
  workflow), and the explicit post-GA list (semantic search, roving tablist,
  axe-core CI, production icon art, telemetry). Whole workspace green: fmt
  --check / clippy --all-targets -D warnings / test (26 suites ok) / deny / npm
  build. GA reached — Core Parity + Fast-follow + AI, signed, auto-updating,
  accessible (WCAG AA), perf-budgeted, cross-platform. Phase 6 is the final
  planned phase; remaining items are post-GA.
